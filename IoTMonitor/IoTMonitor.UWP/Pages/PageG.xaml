<Page
    x:Class="IoTMonitor.UWP.Pages.PageC"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IoTMonitor.UWP.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:IoTMonitor.UWP.Models"
    xmlns:yoga="using:IoTMonitor.UWP"
    mc:Ignorable="d">
    <Page.Resources>
        
    </Page.Resources>

    <Grid Background="{ThemeResource LeftSectionColor}">
        
        <ScrollViewer Name="Scroller">
            <Grid >
                <Grid.RowDefinitions>
                    <RowDefinition Height="116" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Background="{StaticResource LeftSectionColor}" />
                <Border Grid.Row="1" Grid.RowSpan="2" Background="{StaticResource LeftSectionColor}"/>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
